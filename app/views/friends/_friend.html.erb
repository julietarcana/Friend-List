<div class="jumbotron">
  <h1 class="display-4"><%= friend.first_name %>  <%= friend.last_name %></h1>
  <p class="lead"><%= friend.email %> | <%= friend.phone %> | <%= friend.instagram %> </p>
    <%= link_to "Edit this friend", edit_friend_path(@friend), class: "btn btn-primary" %>   
    <%= link_to 'Delete', @friend, method: :delete, data: { turbo_method: :delete, turbo_confirm: 'are you sure?' }, class:"btn btn-danger" %>
    <%= link_to "Back to friends", friends_path,class: "btn btn-outline-primary" %></p>
    <%#= button_to "Remove this friend", @friend, method: :delete, class: "btn btn-danger" %>
</div>

<div id="<%= dom_id friend %>">
</div>
